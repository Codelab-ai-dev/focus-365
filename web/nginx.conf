server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Tolera cookies grandes acumuladas en localhost por otros proyectos
    large_client_header_buffers 8 32k;

    # API: proxy al backend (mismo origen → cookies httpOnly y rutas relativas)
    location /api/ {
        proxy_pass http://api:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # El service worker y el manifest deben revalidarse siempre, para que el
    # navegador detecte versiones nuevas (los assets con hash sí van con caché larga).
    location = /sw.js                 { add_header Cache-Control "no-cache"; }
    location = /registerSW.js         { add_header Cache-Control "no-cache"; }
    location = /manifest.webmanifest  { add_header Cache-Control "no-cache"; }

    # SPA: todas las rutas caen en index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
