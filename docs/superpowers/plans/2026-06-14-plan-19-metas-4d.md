# Plan 19 — Alinear las dimensiones de Metas a las 4D — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Las dimensiones de Metas pasan de checkin/finanzas/entrenamiento/mente/general a las 4D de Capitanes (espiritual/emocional/fisica/financiera).

**Architecture:** Migración 0016 mapea las metas existentes y agrega un CHECK; el validador de metas, el `crear_meta` de la IA (map + enum + prompt) y el selector de `/metas` se actualizan a las 4D.

**Tech Stack:** Go + chi + sqlc/pgx (api), React + Vitest (web).

**Spec:** `docs/superpowers/specs/2026-06-14-plan-19-metas-4d-design.md`

**Entorno:** Go desde `api/` con `GOTOOLCHAIN=local PATH="$PATH:/usr/local/go/bin" TEST_DATABASE_URL="postgres://focus:changeme@localhost:5544/focus365?sslmode=disable"`. Frontend `cd web && npx vitest run && npm run build`. Commits en español con `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Rama: `plan-19-metas-4d` desde `main`.

**Hechos del código (verificados):**
- `goals.dimension` es TEXT sin CHECK (migración 0006). Validado por el handler: `oneof=checkin finanzas entrenamiento mente general` (en `createReq` y `patchReq`).
- `ai/actions.go`: `goalDimensions` map (línea ~88), tool `crear_meta` con su `enum` y `Description` (línea ~636).
- `web/src/routes/metas.tsx`: `const DIMENSIONS = ["checkin", ...]` (línea 23), `useState("general")` (línea 47), `<select>` que mapea `DIMENSIONS` a `<option>` mostrando el valor crudo, y un `<Chip>{g.dimension}</Chip>` (línea ~178).
- El prompt (`chatprompt.go`) no nombra dimensiones específicas (solo «con su dimension»); no requiere cambio de valores, pero sí mantenerse coherente.

---

### Task 1: Migración 0016 + validación de metas

**Files:**
- Create: `api/db/migrations/0016_metas_4d.sql`
- Modify: `api/internal/goals/handler.go`
- Test: `api/internal/store/goals_test.go` (o el del paquete store; verificar el mapeo + CHECK), `api/internal/goals/handler_test.go` (validación)

- [ ] **Step 1: Migración** `api/db/migrations/0016_metas_4d.sql`:

```sql
-- +goose Up
UPDATE goals SET dimension = CASE dimension
    WHEN 'finanzas'      THEN 'financiera'
    WHEN 'entrenamiento' THEN 'fisica'
    WHEN 'mente'         THEN 'emocional'
    WHEN 'checkin'       THEN 'emocional'
    WHEN 'general'       THEN 'espiritual'
    ELSE dimension
END;
ALTER TABLE goals ADD CONSTRAINT goals_dimension_valid
    CHECK (dimension IN ('espiritual','emocional','fisica','financiera'));

-- +goose Down
ALTER TABLE goals DROP CONSTRAINT goals_dimension_valid;
```

- [ ] **Step 2: Test que falla** (verifica el mapeo de datos y el CHECK). En el
test de store de goals (leer `api/internal/store/goals_test.go` o el del
paquete; si no existe uno de goals en store, crear
`api/internal/store/goals_dimension_test.go`):

```go
func TestGoalsDimensionCheck(t *testing.T) {
	pool := testutil.NewDB(t)
	q := store.New(pool)
	ctx := context.Background()
	u, err := q.CreateUser(ctx, store.CreateUserParams{Email: "g4d@b.com", PasswordHash: "h", Name: "G"})
	if err != nil {
		t.Fatalf("CreateUser: %v", err)
	}
	// Crear una meta con una 4D válida pasa.
	if _, err := q.CreateGoal(ctx, store.CreateGoalParams{
		UserID: u.ID, Title: "Ahorrar", Dimension: "financiera",
	}); err != nil {
		t.Fatalf("dimensión 4D válida rechazada: %v", err)
	}
	// Una dimensión fuera de las 4D (vieja) viola el CHECK.
	if _, err := q.CreateGoal(ctx, store.CreateGoalParams{
		UserID: u.ID, Title: "X", Dimension: "general",
	}); err == nil {
		t.Error("dimensión 'general' debería violar el CHECK")
	}
}
```

(Verificar la firma real de `CreateGoalParams` —puede requerir `Deadline`
*time.Time nullable—; ajustar. `testutil.NewDB` aplica la 0016.)

- [ ] **Step 3: Verificar que falla** (antes de la migración el `general`
  pasaría; correr el test confirma que la 0016 ya está y el CHECK actúa —si el
  test pasa directo es porque testutil aplicó la migración: correr para ver
  verde tras escribir la migración).

```bash
cd /Users/gustavo/Desktop/focus-365/api && GOTOOLCHAIN=local PATH="$PATH:/usr/local/go/bin" TEST_DATABASE_URL="postgres://focus:changeme@localhost:5544/focus365?sslmode=disable" go test ./internal/store/ -run TestGoalsDimensionCheck -v
```

- [ ] **Step 4: Validación del handler.** En `api/internal/goals/handler.go`,
los dos tags `oneof`:

```go
// createReq:
Dimension string `json:"dimension" validate:"required,oneof=espiritual emocional fisica financiera"`
// patchReq:
Dimension *string `json:"dimension" validate:"omitempty,oneof=espiritual emocional fisica financiera"`
```

- [ ] **Step 5: Test del handler.** En `api/internal/goals/handler_test.go`, los
tests que crean metas con dimensiones viejas (`finanzas`, `general`, etc.) se
adaptan a las 4D; agregar uno que rechace `general`:

```go
func TestCreateGoalRejectsOldDimension(t *testing.T) {
	// POST /goals con dimension "general" → 400 (ya no es válida).
	// (Seguir el harness del archivo; assert rec.Code == http.StatusBadRequest.)
}
```

Y los tests felices existentes usan una 4D (p. ej. `financiera`).

- [ ] **Step 6: Verificar + commit**

```bash
cd /Users/gustavo/Desktop/focus-365/api && GOTOOLCHAIN=local PATH="$PATH:/usr/local/go/bin" TEST_DATABASE_URL="postgres://focus:changeme@localhost:5544/focus365?sslmode=disable" go test ./internal/store/ ./internal/goals/ -count=1
git add api/db/migrations/0016_metas_4d.sql api/internal/goals
git commit -m "feat(goals): dimensiones 4D (migración + CHECK + validación)"
```

(El build de `./...` debería seguir compilando —el AI usa otra lista; se alinea en la Task 2.)

---

### Task 2: IA — `crear_meta` con las 4D

**Files:**
- Modify: `api/internal/ai/actions.go`
- Test: `api/internal/ai/actions_test.go`

- [ ] **Step 1: Tests que fallan** en `actions_test.go`:

```go
func TestParseMetaNuevaDimensiones4D(t *testing.T) {
	for _, dim := range []string{"espiritual", "emocional", "fisica", "financiera"} {
		args := `{"title":"X","dimension":"` + dim + `"}`
		if _, err := parseActionPayload("meta_nueva", args); err != nil {
			t.Errorf("dimensión %s debería ser válida: %v", dim, err)
		}
	}
	// Las viejas ya no.
	for _, dim := range []string{"general", "finanzas", "mente", "checkin", "entrenamiento"} {
		args := `{"title":"X","dimension":"` + dim + `"}`
		if _, err := parseActionPayload("meta_nueva", args); err == nil {
			t.Errorf("dimensión vieja %s debería rechazarse", dim)
		}
	}
}

func TestCrearMetaToolEnum4D(t *testing.T) {
	tools := buildChatTools()
	var schema string
	for _, tl := range tools {
		if tl.Name == "crear_meta" {
			schema = string(tl.Parameters)
		}
	}
	for _, dim := range []string{"espiritual", "emocional", "fisica", "financiera"} {
		if !strings.Contains(schema, dim) {
			t.Errorf("el enum de crear_meta debe incluir %s: %s", dim, schema)
		}
	}
	if strings.Contains(schema, "general") || strings.Contains(schema, "finanzas") {
		t.Errorf("el enum no debe traer dimensiones viejas: %s", schema)
	}
}
```

- [ ] **Step 2: Verificar que fallan.**

- [ ] **Step 3: Implementar** en `actions.go`:

`goalDimensions`:

```go
// goalDimensions replica el oneof del handler HTTP de metas (las 4 dimensiones
// de Capitanes).
var goalDimensions = map[string]bool{
	"espiritual": true, "emocional": true, "fisica": true, "financiera": true,
}
```

Tool `crear_meta` (Description + enum):

```go
{
	Name:        "crear_meta",
	Description: "Crea una meta nueva. dimension es una de las 4 dimensiones: espiritual (identidad, propósito, conexión, visión), emocional (sanidad, autocontrol, mente), fisica (cuerpo, ejercicio, energía, disciplina) o financiera (dinero, ahorro, multiplicación). Infiérela del tema; elige siempre la más cercana. deadline opcional en YYYY-MM-DD.",
	Parameters: json.RawMessage(`{"type":"object","properties":{
		"title":{"type":"string"},
		"dimension":{"type":"string","enum":["espiritual","emocional","fisica","financiera"]},
		"deadline":{"type":"string","description":"YYYY-MM-DD, opcional"}},
		"required":["title","dimension"]}`),
},
```

- [ ] **Step 4: Verificar backend completo + commit**

```bash
cd /Users/gustavo/Desktop/focus-365/api && GOTOOLCHAIN=local PATH="$PATH:/usr/local/go/bin" go vet ./... && GOTOOLCHAIN=local PATH="$PATH:/usr/local/go/bin" TEST_DATABASE_URL="postgres://focus:changeme@localhost:5544/focus365?sslmode=disable" go test -p 1 ./... -count=1
git add api/internal/ai/actions.go api/internal/ai/actions_test.go
git commit -m "feat(ai): crear_meta usa las 4 dimensiones de Capitanes"
```

---

### Task 3: Frontend — selector de `/metas` con las 4D

**Files:**
- Modify: `web/src/routes/metas.tsx`
- Test: `web/src/routes/metas.test.tsx`

- [ ] **Step 1: Tests que fallan/adaptan** en `metas.test.tsx`: el selector
muestra las 4 etiquetas (Espiritual/Emocional/Física/Financiera); crear una meta
manda el valor en minúscula correcto. Leer el harness; adaptar los mocks/asserts
que usaban dimensiones viejas. Ejemplo:

```tsx
it("el selector de dimensión ofrece las 4D y guarda el valor en minúscula", async () => {
  // mock POST /goals que captura el body
  renderPage();
  const select = await screen.findByLabelText("Dimensión");
  // las 4 opciones visibles
  expect(screen.getByRole("option", { name: "Física" })).toBeInTheDocument();
  await userEvent.selectOptions(select, "fisica");
  await userEvent.type(screen.getByLabelText(/título/i), "Correr 5k");
  await userEvent.click(screen.getByRole("button", { name: /crear|guardar/i }));
  // el body POSTeado lleva dimension: "fisica"
});
```

(Ajustar labels/roles exactos al componente real.)

- [ ] **Step 2: Implementar** en `metas.tsx`:

Reemplazar `DIMENSIONS`:

```ts
// Las 4 dimensiones de Capitanes: etiqueta visible → valor almacenado.
const DIMENSIONS: { value: string; label: string }[] = [
  { value: "espiritual", label: "Espiritual" },
  { value: "emocional", label: "Emocional" },
  { value: "fisica", label: "Física" },
  { value: "financiera", label: "Financiera" },
];
const DIM_LABEL: Record<string, string> = Object.fromEntries(
  DIMENSIONS.map((d) => [d.value, d.label])
);
```

Default del estado: `useState("espiritual")` (en vez de `"general"`).

El `<select>`:

```tsx
{DIMENSIONS.map((d) => (
  <option key={d.value} value={d.value}>{d.label}</option>
))}
```

El chip de cada meta muestra la etiqueta:

```tsx
<Chip variant="plain" size="sm">{DIM_LABEL[g.dimension] ?? g.dimension}</Chip>
```

- [ ] **Step 3: Suite completa + build + commit**

```bash
cd /Users/gustavo/Desktop/focus-365/web && npx vitest run && npm run build
git add web/src/routes/metas.tsx web/src/routes/metas.test.tsx
git commit -m "feat(web): selector de metas con las 4 dimensiones (etiquetas bonitas)"
```

---

### Task 4: Cierre — review, merge, deploy, smoke de producción

- [ ] **Step 1:** Suites completas (backend `-p 1 ./...` + frontend + build) y smoke local de acciones (`/tmp/smoke_actions.sh`).
- [ ] **Step 2:** Rebuild docker; smoke local: crear una meta vía `POST /goals` con `dimension:"financiera"` → 200; con `dimension:"general"` → 400.
- [ ] **Step 3:** Review final holística (subagente), nits.
- [ ] **Step 4:** Merge `--no-ff` a `main` + push (la GitHub App ya conecta el repo; verificar que el auto-deploy dispare —si no, Deploy manual; la migración 0016 mapea las metas viejas al arrancar).
- [ ] **Step 5:** Smoke de producción: crear una meta con dimensión «financiera» → aparece en `/goals`; las metas viejas del usuario quedan en una de las 4D (verificar con `GET /goals`).
- [ ] **Step 6:** Bitácora en `docs/superpowers/sesiones/` y push.

---

## Notas para el ejecutor

- `goals.dimension` no tenía CHECK; la 0016 lo agrega tras mapear, así datos viejos no lo violan. Si en producción hubiera un valor inesperado fuera de la lista vieja, caería en el `ELSE` y violaría el CHECK → la migración fallaría ruidosamente (correcto: avisa).
- Las 4 acciones del chat, el check-in y los compromisos son INVARIANTES; solo cambia la enum de dimensión de metas.
- El `countActive` del dashboard NO se toca (cuenta las 5 áreas de la app, no la dimensión de la meta).
